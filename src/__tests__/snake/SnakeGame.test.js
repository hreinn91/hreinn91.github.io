

/* test("Simple", () => {
    let direction = [1, 0];
    let queue = [[100, 100]];
    let nextQueue = getNextQueue(queue, direction, isFalse);
    expect(nextQueue)
        .toStrictEqual([[120, 100]]);
});



test("Straight line", () => {
    let direction = [1, 0];
    let queue = [[130, 100], [120, 100], [110, 100], [100, 100]];
    let nextQueue = getNextQueue(queue, direction, isFalse);
    expect(nextQueue)
        .toStrictEqual([[140, 100], [130, 100], [120, 100], [110, 100]]);
}); */